import 'package:flutter/material.dart';

class NoticeLatestNews extends StatelessWidget {
  const NoticeLatestNews({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}