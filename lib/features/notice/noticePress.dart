import 'package:flutter/material.dart';

class NoticePress extends StatelessWidget {
  const NoticePress({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}