import 'package:flutter/material.dart';

class NoticeAll extends StatelessWidget {
  const NoticeAll({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
